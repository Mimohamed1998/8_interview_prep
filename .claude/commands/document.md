# /document — Workflow Documentation Generator

Creates a standardised `.md` file in `docs/` for a notebook step.

**Usage:** `/document <filename> [--title "Title"]`

See `.claude/skills/document.md` for full instructions.
